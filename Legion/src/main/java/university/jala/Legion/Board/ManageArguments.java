package university.jala.Legion.Board;

public interface ManageArguments<M> {
    M validate(String arg);
    /**
     * this interface had the method validated
     * @param arg is the parameter
     */
}
